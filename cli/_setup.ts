import '../abstractions/global-vars';
import '../abstractions/attribute-interfaces';
import '../abstractions/fn';

// initialize global vars

// would be cleaner to call this where we need product types
// but for compat, we simply call it here once, this will initialize the
// global "pt" variable
productTypes();
