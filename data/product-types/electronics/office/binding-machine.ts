
import { ag } from '../../../attribute-groups';

PType({
    name: 'Binding machine',
    key: 'bindingmachine',
    categories: ['officeElectronics'],
    attributeRefs: [
        ...ag.common,
    ]
});