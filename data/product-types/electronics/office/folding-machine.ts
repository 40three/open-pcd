
import { ag } from '../../../attribute-groups';

PType({
    name: 'Folding machine',
    key: 'foldingmachine',
    categories: ['officeElectronics'],
    attributeRefs: [
        ...ag.common,
    ]
});