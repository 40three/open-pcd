
import { ag } from '../../../attribute-groups';

PType({
    name: 'Labeling machine',
    key: 'labelingmachine',
    categories: ['officeElectronics'],
    attributeRefs: [
        ...ag.common,
    ]
});