
import { ag } from '../../../attribute-groups';

PType({
    name: 'Drilling machine',
    key: 'drillingmachine',
    categories: ['homeCinema'],
    attributeRefs: [
        ...ag.common,
    ]
});