
import { ag } from '../../../attribute-groups';

PType({
    name: 'Extraction system',
    key: 'extractionsystem',
    categories: ['homeCinema'],
    attributeRefs: [
        ...ag.common,
    ]
});