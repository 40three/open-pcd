
import { ag } from '../../../attribute-groups';

PType({
    name: 'Lawn edge trimmer',
    key: 'lawnedgetrimmer',
    categories: ['homeCinema'],
    attributeRefs: [
        ...ag.common,
    ]
});