
import { ag } from '../../../attribute-groups';

PType({
    name: 'Poloshing machine',
    key: 'poloshingmachine',
    categories: ['smartHome'],
    attributeRefs: [
        ...ag.common,
    ]
});