
import { ag } from '../../../attribute-groups';

PType({
    name: 'Binding machine',
    key: 'bindingmachine',
    categories: ['officeSupplies'],
    attributeRefs: [
        ...ag.common,
    ]
});