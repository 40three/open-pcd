
import { ag } from '../../../attribute-groups';

PType({
    name: 'Transparent folder',
    key: 'transparentfolder',
    categories: ['stationery'],
    attributeRefs: [
        ...ag.common,
    ]
});